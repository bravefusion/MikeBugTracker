@model MikeBugTracker.Models.Project
@using MikeBugTracker.Helpers

@{ 
    var ticketHelper = new TicketHelper();
}

<!-- Default box -->
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Projects Detail</h3>

        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                <i class="fas fa-minus"></i>
            </button>
            <button type="button" class="btn btn-tool" data-card-widget="remove" data-toggle="tooltip" title="Remove">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12 col-md-12 col-lg-8 order-2 order-md-1">
                <div class="row">
                    <div class="col-12 col-sm-4">
                        <div class="info-box bg-light">
                            <div class="info-box-content">
                                <span class="info-box-text text-center text-muted">@Html.DisplayNameFor(model => model.Name)</span>
                                <span class="info-box-number text-center text-muted mb-0">@Html.DisplayFor(model => model.Name)</span>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-sm-4">
                        <div class="info-box bg-light">
                            <div class="info-box-content">
                                <span class="info-box-text text-center text-muted">@Html.DisplayNameFor(model => model.Description)</span>
                                <span class="info-box-number text-center text-muted mb-0">@Html.DisplayFor(model => model.Description)</span>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-sm-4">
                        <div class="info-box bg-light">
                            <div class="info-box-content">
                                <span class="info-box-text text-center text-muted">Total Tickets</span>
                                <span class="info-box-number text-center text-muted mb-0">
                                    @if (Model.Tickets.Count() != 0)
                                    {
                                        @Model.Tickets.Count()
                                    }
                                    
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <h3 class="text-primary"><i class="fas fa-"></i> Users On Project</h3>
                        @*<table id="thetable" class="table">
                                <thead>
                                    <tr>
                                        <th>
                                            First Name
                                        </th>
                                        <th>
                                            Last Name
                                        </th>



                                    </tr>
                                </thead>

                                @foreach (var item in Model.Users)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.FirstName)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.LastName)
                                        </td>


                                    </tr>
                                }
                            </table>*@
                        <div>

                        </div>
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Striped Full Width Table</h3>
                            </div>
                            <!-- /.card-header -->
                            <div class="card-body p-0">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>
                                            </th>
                                            <th>
                                                First Name
                                            </th>
                                            <th>
                                                Last Name
                                            </th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @{
                                            int rowCount = 1;
                                            foreach (var item in Model.Users)
                                            {
                                                <tr>
                                                    <td>@rowCount.ToString()</td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.FirstName)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.LastName)
                                                    </td>

                                                    <td>@Html.ActionLink("Profile", "EditProfile", "Manage", new { id = item.Id }, new { @class = "badge bg-info" })</td>
                                                    <td>
                                                        @Html.ActionLink("Edit", "ManageProjectUsers", "Admin", new { id = item.Id }, new { @class = "badge bg-warning" })
                                                    </td>
                                                </tr>
                                                rowCount++;
                                            }}
                                    </tbody>
                                </table>
                            </div>
                            <!-- /.card-body -->
                        </div>
                        <!-- /.card -->
                    </div>
                    <!-- /.col -->
                </div>





            </div>
        </div>
    </div>
    <div class="col-12 col-md-12 col-lg-4 order-1 order-md-2">
        <h3 class="text-primary"><i class="fas fa-paint-brush"></i> Tickets On Project</h3>
        <p class="text-muted"></p>
        <br>
        @foreach (var ticket in Model.Tickets)
        {
            <div class="post">
                <div class="user-block">
                    <img class="img-circle img-bordered-sm" src="../../dist/img/user1-128x128.jpg" alt="">
                    <span class="username">
                        <a href="#">@ticket.Title</a>
                    </span>
                    <span class="description">@ticket.Created</span>
                </div>
                <!-- /.user-block -->
                <p>
                    @ticket.Description
                </p>

                
            </div>
        }

        
    </div>
</div>
    
<!-- /.card-body -->




<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
